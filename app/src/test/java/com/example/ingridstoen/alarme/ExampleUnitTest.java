package com.example.ingridstoen.alarme;

import android.view.View;
import org.junit.Test;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */

public class ExampleUnitTest {
    /*
    @Test
    public void login() throws Exception {
        String username = "ingridbs";
        String password = "In0292*s";
        List<String> courses = new Database_Courses().execute().get();
        assertEquals(new ArrayList<>(Arrays.asList("TTM4100", "TFY4125", "TDT4145")), new ArrayList(Arrays.asList(courses)));
    }
    */
}